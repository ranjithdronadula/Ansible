- name: install packages
  hosts: frontend
  become: yes
  tasks:
  - name: install packages
    ansible.builtin.package:
      msg: "{{ item }}"
      state: installed
    loop:
    - nginx
    - mysql
    - zip